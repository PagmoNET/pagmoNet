org.gradle.parallel=true
org.gradle.caching=true

# Single source of truth for the published coordinates. CI overrides version at
# invocation with -Pversion=<tag>; this is the local/default value.
group=io.github.pagmonet
version=1.0.0
